import 'package:flutter/cupertino.dart';

const elevationMin = 3.0;
const elevationMid = 5.0;
const elevationMax = 7.0;

const vsMin = SizedBox(height: 12);
const vsMid = SizedBox(height: 24);
const vsMax = SizedBox(height: 32);

const hsMin = SizedBox(width: 12);
const hsMid = SizedBox(width: 24);
const hsMax = SizedBox(width: 32);

const vpMin = EdgeInsets.symmetric(vertical: 12);
const vpMid = EdgeInsets.symmetric(vertical: 24);
const vpMax = EdgeInsets.symmetric(vertical: 32);

const hpMin = EdgeInsets.symmetric(horizontal: 12);
const hpMid = EdgeInsets.symmetric(horizontal: 24);
const hpMax = EdgeInsets.symmetric(horizontal: 32);

const pMin = EdgeInsets.all(12);
const pMid = EdgeInsets.all(24);
const pMax = EdgeInsets.all(32);

const brMin = BorderRadius.all(Radius.circular(12));
const brMid = BorderRadius.all(Radius.circular(24));
const brMax = BorderRadius.all(Radius.circular(32));

const rrbMin = RoundedRectangleBorder(borderRadius: brMin);
const rrbMid = RoundedRectangleBorder(borderRadius: brMid);
const rrbMax = RoundedRectangleBorder(borderRadius: brMax);
